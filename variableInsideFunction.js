function setVar() {
	// New scope. 

	// We can't acces to this variable outside this scope.
	var myName = 'Cristina Rojas';
}

setVar();

// We can't acces to the function scope (inside scop
console.log(myName); e). // myName is not definded.